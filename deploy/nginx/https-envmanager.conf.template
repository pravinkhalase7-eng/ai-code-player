# HTTPS for manager.doxstation.com (Env Manager) — written at container start if certs exist.

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name manager.doxstation.com;
    client_max_body_size 8m;

    ssl_certificate     /etc/letsencrypt/live/manager.doxstation.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/manager.doxstation.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    add_header Strict-Transport-Security "max-age=31536000" always;

    location / {
        proxy_pass http://envmanager_app;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
        proxy_buffering off;
    }
}
